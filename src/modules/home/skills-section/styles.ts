import { css } from "@emotion/react";

export const skillsSectionStyles = css`
  display: flex;
  flex-direction: column;
  gap: 8px;

  > .title {
    all: unset;
    font-weight: bold;
    font-size: 40px;
    color: var(--gunmetal);
  }

  > .content {
  }
`;
